# Core dependencies (lean core)
fastapi>=0.110.0
uvicorn>=0.28.0
pydantic>=2.0.0
numpy>=1.24.0
requests>=2.31.0

# For local development with LanceDB & FastEmbed:
# pip install -e ".[local,dev]"
#
# For production with OpenAI-compatible proxies & MariaDB:
# pip install -e ".[openai,mariadb]"
